/**
 * Re-export estável de updateClinicAddress para o grafo ESM de Production.
 * Sem persistência própria e sem side effect no import.
 */
export { updateClinicAddress } from './clinicService.js';
